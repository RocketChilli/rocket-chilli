version: '3.7'

networks:
  default:
    name: localdev

services:
  web:
    build:
      context: ./
    container_name: rocketchilli-web
    networks:
      - default
    volumes:
      - ./:/usr/local/apache2/htdocs
    ports:
      - 80
      - 35729:35729
    environment:
      VIRTUAL_HOST: local.rocketchilli.com
